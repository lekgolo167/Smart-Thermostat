#pragma once

#include <queue>

inline std::queue<int> *global_msg_queue;

enum ThermostatMessages {
	SAMPLE_AIR,
	RTC_UPDATE,
	UPDATE_SAMPLE_PERIOD,
	MOTION_DETECTED,
	NO_MOTION,
	SEND_SERVER_MSG,
	SEND_SERVER_TEMPERATURE,
	SEND_SERVER_STATS,
	SEND_SERVER_MOTION,
	SEND_SEVER_RUNTIME,
	SEND_SERVER_FURNACE_STATE,
	SAVE_RUNTIME,
	OLED_ON,
	OLED_OFF,
	OLED_UPDATE,
	OLED_NEXT_MENU,
	OLED_PREV_MENU,
	OLED_EDIT_MENU,
	OLED_ROTARY_CCW,
	OLED_ROTARY_CW
};
